# Platform-Specific Libraries

Place prebuilt static libraries for each platform in the appropriate subdirectory:

- **win64/**: Windows x64 libraries (`.lib` files)
- **mac/**: macOS libraries (`.a` files)
- **linux/**: Linux libraries (`.a` files)

See parent README.md for instructions on obtaining these libraries.
